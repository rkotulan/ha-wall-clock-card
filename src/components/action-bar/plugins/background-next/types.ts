import { ModuleActionConfig } from '../../types';

/**
 * Action ID for the background next action
 */
export const BACKGROUND_NEXT_ACTION = 'background-next';

/**
 * Configuration for the background next action
 */
export interface BackgroundNextActionConfig extends ModuleActionConfig {
    // No additional properties needed for this action
}