import {ModuleActionConfig} from '../../types';

/**
 * Configuration for a weather update action
 */
export interface WeatherUpdateActionConfig extends ModuleActionConfig {
    // No additional properties needed for weather update action
    // It simply inherits the base properties from ModuleActionConfig
}